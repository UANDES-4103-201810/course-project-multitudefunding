<h1>New Project</h1>
<% if user_signed_in? %>
<%= render 'create_form', project: @project %>

<%= link_to 'Back', projects_path %>
<% else %>
<h2> You must have an account to be able to create a new project </h2>
<h3><%= link_to('Login', new_user_session_path) %> if you have an account</h2>
<h3> or <%= link_to 'Sign Up', new_user_registration_path %> if you're new </h3>
<% end %>
